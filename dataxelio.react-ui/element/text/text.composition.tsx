import React from "react";
import { Text } from "./text";

// sets the Component preview in gallery view
export const BasicText = () => {
  return <Text inheritStyle={false} tText="hello from Text"></Text>;
};
