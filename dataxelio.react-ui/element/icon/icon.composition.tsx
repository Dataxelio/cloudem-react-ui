import React from "react";
import { Icon } from "./icon";

// sets the Component preview in gallery view
export const BasicIcon = () => {
  return (
    <Icon iName="folder" inheritStyle={false}>
      hello from Icon
    </Icon>
  );
};
