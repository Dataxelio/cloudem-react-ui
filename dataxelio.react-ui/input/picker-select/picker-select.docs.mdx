# Picker Select

---

labels: ['react', 'typescript', 'picker-select']
description: 'A PickerSelect component.'

---

import { PickerSelect } from "./picker-select";

PickerSelect example:

A simple PickerSelect component with some text

Code Snippet:

```js
<PickerSelect>hello from PickerSelect</PickerSelect>
```

Live Playground:

```js live
<PickerSelect>hello from PickerSelect</PickerSelect>
```
