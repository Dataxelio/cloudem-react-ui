import React from "react";
import { FormGroup } from "./form-group";

// sets the Component preview in gallery view
export const BasicFormGroup = () => {
  return (
    <FormGroup label="Email" labelFor="Email">
      hello from FormGroup
    </FormGroup>
  );
};
