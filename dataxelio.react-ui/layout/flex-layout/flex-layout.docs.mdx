# Flex Layout

---

labels: ['react', 'typescript', 'flex-layout']
description: 'A FlexLayout component.'

---

import { FlexLayout } from "./flex-layout";

FlexLayout example:

A simple FlexLayout component with some text

Code Snippet:

```ts
<FlexLayout>hello from FlexLayout</FlexLayout>
```

Live Playground:

```ts live
<FlexLayout>hello from FlexLayout</FlexLayout>
```
